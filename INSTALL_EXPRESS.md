# 📦 Установка Express.js

## Быстрая установка

Express.js уже добавлен в `package.json`. Просто установите зависимости:

```bash
npm install
```

Это установит:
- `express` - веб-фреймворк для REST API
- Все остальные зависимости проекта

## Проверка установки

После установки проверьте, что Express установлен:

```bash
# Windows PowerShell
dir node_modules\express

# Linux/macOS
ls node_modules/express
```

Или проверьте версию:

```bash
npm list express
```

Ожидаемый результат:
```
maxbotPA@1.0.0
└── express@4.18.2
```

## Запуск сервера

После успешной установки запустите сервер:

```bash
npm run server
```

Вы должны увидеть:

```
=============================================================
🚀 AI Business Bot Server запущен!
=============================================================
📡 Сервер слушает порт: 3000
🌐 URL: http://localhost:3000
💚 Health Check: http://localhost:3000/health
📖 API Docs: http://localhost:3000/
=============================================================
```

## Проверка работы

Откройте в браузере или используйте curl:

```bash
curl http://localhost:3000/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": "2025-12-25T...",
  "uptime": 12.34,
  "environment": "development"
}
```

## Альтернативный способ установки

Если `npm install` не работает, попробуйте установить Express напрямую:

```bash
npm install express@4.18.2
```

## Проблемы с установкой?

### Windows PowerShell - проблемы с кодировкой

Если видите ошибки с кодировкой в PowerShell:

1. Попробуйте использовать **Command Prompt (cmd.exe)** вместо PowerShell:
   ```cmd
   npm install
   ```

2. Или используйте **Git Bash** (если установлен Git):
   ```bash
   npm install
   ```

3. Или запустите PowerShell с другой кодировкой:
   ```powershell
   [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
   npm install
   ```

### Ошибки прав доступа

Если получаете ошибки EACCES или EPERM:

```bash
# Очистите кеш npm
npm cache clean --force

# Попробуйте снова
npm install
```

### Proxy или firewall

Если находитесь за корпоративным прокси:

```bash
npm config set proxy http://proxy.company.com:8080
npm config set https-proxy http://proxy.company.com:8080
npm install
```

### Node.js устарел

Убедитесь что используете Node.js 18 или новее:

```bash
node --version
```

Если версия старая, обновите Node.js:
- Windows: Скачайте с [nodejs.org](https://nodejs.org)
- Linux/macOS: Используйте nvm

## После установки

1. Создайте `.env` файл (см. `.env.example` для справки)
2. Заполните все необходимые переменные окружения
3. Запустите сервер: `npm run server`
4. Откройте в браузере: `http://localhost:3000`

## Дальнейшие шаги

- [SERVER_README.md](./SERVER_README.md) - Руководство по использованию сервера
- [API_DOCS.md](./API_DOCS.md) - Документация REST API
- [examples/](./examples/) - Примеры использования

---

**Если проблемы остались, создайте Issue в GitHub с описанием ошибки.**



# 🚀 Запуск автопостинга

## На Windows (локальный компьютер)

### Способ 1: BAT файл (самый простой)
```
Двойной клик на: start-with-scheduler.bat
```

### Способ 2: PowerShell команда
```powershell
$env:AUTO_START_SCHEDULER='true'; node server.js
```

### Способ 3: Через .env файл
```powershell
# 1. Копировать шаблон
Copy-Item .env.example .env

# 2. Открыть и изменить AUTO_START_SCHEDULER=false на true
notepad .env

# 3. Запустить
node server.js
```

---

## На Linux сервере (VPS/Production)

### Способ 1: PM2 (рекомендуется для production)

```bash
# 1. Установить PM2 (если не установлен)
npm install -g pm2

# 2. Создать папку для логов
mkdir -p logs

# 3. Запустить бота с автозапуском планировщика
pm2 start ecosystem.config.js

# 4. Сохранить настройки PM2
pm2 save

# 5. Настроить автозапуск при перезагрузке сервера
pm2 startup

# 6. Проверить статус
pm2 status
pm2 logs ai-bot
```

### Способ 2: systemd (альтернатива)

```bash
# 1. Создать .env файл
cp .env.example .env
nano .env
# Изменить AUTO_START_SCHEDULER=false на true

# 2. Запустить
node server.js

# Или в фоне:
nohup node server.js > logs/bot.log 2>&1 &
```

### Способ 3: Docker (если используете)

```bash
docker-compose up -d
```

---

## 📊 Проверка что автопостинг работает

### Через API:
```bash
# Статус планировщика
curl http://localhost:3000/api/scheduler/status

# Статус бота
curl http://localhost:3000/api/bot/status

# Статистика контента
curl http://localhost:3000/api/content/stats
```

### Через браузер:
- http://localhost:3000/ - все endpoints
- http://localhost:3000/health - здоровье сервера
- http://localhost:3000/api/scheduler/status - статус планировщика

---

## 🛑 Остановка автопостинга

### Windows:
```
Ctrl+C в окне терминала
```

### Linux с PM2:
```bash
pm2 stop ai-bot
# или полностью удалить
pm2 delete ai-bot
```

---

## 🔧 Управление планировщиком через API

### Запустить планировщик вручную:
```bash
curl -X POST http://localhost:3000/api/scheduler/start
```

### Опубликовать пост сейчас:
```bash
curl -X POST http://localhost:3000/api/bot/publish
```

### Собрать новости:
```bash
curl -X POST http://localhost:3000/api/content/collect
```

---

## ⏰ Расписание по умолчанию

В `.env` файле настроены 3 публикации в день:
- `CRON_SCHEDULE_1=0 9 * * *` - 09:00
- `CRON_SCHEDULE_2=0 14 * * *` - 14:00
- `CRON_SCHEDULE_3=0 19 * * *` - 19:00

Часовой пояс: `TIMEZONE=Asia/Irkutsk`

---

## ✅ Что происходит при автозапуске

1. ✅ Сервер запускается на порту 3000
2. ✅ Планировщик автоматически активируется
3. ✅ По расписанию:
   - Собираются новости с AI/ML источников
   - Анализируются и суммируются через Claude/GPT
   - Генерируются изображения через OpenAI DALL-E
   - Публикуются в Telegram канал
4. ✅ Логи записываются в консоль и файлы

---

## 🆘 Проблемы и решения

### Планировщик не запускается
```bash
# Проверить переменную окружения
echo $AUTO_START_SCHEDULER  # Linux
$env:AUTO_START_SCHEDULER   # Windows

# Должно быть: true
```

### Проверить что бот работает
```bash
# Просмотр логов PM2
pm2 logs ai-bot --lines 100

# Или через API
curl http://localhost:3000/health
```

### Ручная публикация (если автопостинг не работает)
```bash
curl -X POST http://localhost:3000/api/bot/run
```



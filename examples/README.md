# 📚 Примеры использования API

Эта директория содержит примеры работы с AI Business Bot API на разных языках программирования.

## 📁 Файлы

- `api-usage.js` - Примеры на JavaScript/Node.js
- `api-usage.py` - Примеры на Python
- `api-usage.sh` - Примеры на Bash (curl)

## 🚀 Быстрый старт

### 1. Запустите сервер

```bash
npm run server
```

Сервер запустится на `http://localhost:3000`

### 2. Выберите язык и запустите примеры

## JavaScript/Node.js

```bash
# Установите зависимости (если не установлены)
npm install

# Примеры команд
node examples/api-usage.js health      # Health check
node examples/api-usage.js status      # Статус бота
node examples/api-usage.js stats       # Статистика
node examples/api-usage.js queue       # Очередь постов
node examples/api-usage.js collect     # Собрать новости
node examples/api-usage.js publish     # Опубликовать пост
node examples/api-usage.js run         # Запустить бота
node examples/api-usage.js scheduler   # Управление планировщиком
node examples/api-usage.js monitor     # Мониторинг (бесконечный)
node examples/api-usage.js workflow    # Полный рабочий процесс
```

## Python

```bash
# Установите зависимости
pip install requests

# Примеры команд
python examples/api-usage.py health      # Health check
python examples/api-usage.py status      # Статус бота
python examples/api-usage.py stats       # Статистика
python examples/api-usage.py queue       # Очередь постов
python examples/api-usage.py collect     # Собрать новости
python examples/api-usage.py publish     # Опубликовать пост
python examples/api-usage.py run         # Запустить бота
python examples/api-usage.py scheduler   # Управление планировщиком
python examples/api-usage.py monitor     # Мониторинг (бесконечный)
python examples/api-usage.py workflow    # Полный рабочий процесс
```

## Bash (curl)

```bash
# Сделайте скрипт исполняемым
chmod +x examples/api-usage.sh

# Примеры команд
./examples/api-usage.sh health      # Health check
./examples/api-usage.sh status      # Статус бота
./examples/api-usage.sh stats       # Статистика
./examples/api-usage.sh queue       # Очередь постов
./examples/api-usage.sh collect     # Собрать новости
./examples/api-usage.sh publish     # Опубликовать пост
./examples/api-usage.sh run         # Запустить бота
./examples/api-usage.sh scheduler   # Управление планировщиком
./examples/api-usage.sh workflow    # Полный рабочий процесс

# Для красивого вывода JSON установите jq
# Ubuntu/Debian: sudo apt install jq
# macOS: brew install jq
```

## 🎯 Рекомендуемый рабочий процесс

### Вариант 1: Полный автоматический workflow

Запустите полный рабочий процесс одной командой:

```bash
# JavaScript
node examples/api-usage.js workflow

# Python
python examples/api-usage.py workflow

# Bash
./examples/api-usage.sh workflow
```

Это выполнит:
1. ✅ Health check сервера
2. 🔍 Проверку статуса бота
3. 📊 Получение текущей статистики
4. 📰 Сбор новостей (если очередь пуста)
5. 📤 Публикацию тестового поста
6. ⏰ Запуск автоматического планировщика

### Вариант 2: Пошаговое управление

```bash
# 1. Проверьте здоровье сервера
node examples/api-usage.js health

# 2. Соберите новости
node examples/api-usage.js collect

# 3. Проверьте очередь
node examples/api-usage.js queue

# 4. Опубликуйте пост
node examples/api-usage.js publish

# 5. Запустите планировщик для автоматизации
node examples/api-usage.js scheduler
```

### Вариант 3: Непрерывный мониторинг

Запустите мониторинг для отслеживания состояния бота:

```bash
# JavaScript
node examples/api-usage.js monitor

# Python
python examples/api-usage.py monitor
```

Мониторинг будет:
- Проверять статистику каждые 5 минут
- Автоматически собирать новости если очередь пуста
- Предупреждать если постов в очереди меньше 5

## 🔧 Настройка URL

По умолчанию примеры подключаются к `http://localhost:3000`.

Чтобы изменить URL:

```bash
# JavaScript
API_URL=https://your-domain.com node examples/api-usage.js health

# Python
export API_URL=https://your-domain.com
python examples/api-usage.py health

# Bash
API_URL=https://your-domain.com ./examples/api-usage.sh health
```

## 📖 Подробная документация

- [API_DOCS.md](../API_DOCS.md) - Полная документация API
- [DEPLOY.md](../DEPLOY.md) - Инструкции по деплою
- [SERVER_README.md](../SERVER_README.md) - Руководство по запуску сервера

## 💡 Полезные советы

1. **Для разработки** используйте `npm run server:dev` для автоматической перезагрузки сервера при изменениях

2. **Для продакшена** установите `AUTO_START_SCHEDULER=true` в `.env` для автоматического запуска планировщика

3. **Для мониторинга** используйте команду `monitor` для отслеживания состояния бота в реальном времени

4. **Для тестирования** сначала запустите `workflow` чтобы убедиться что всё настроено правильно

## 🆘 Проблемы?

Если примеры не работают:

1. Убедитесь, что сервер запущен: `npm run server`
2. Проверьте что порт 3000 не занят: `lsof -i :3000` (Linux/macOS)
3. Проверьте логи сервера на наличие ошибок
4. Убедитесь, что все переменные окружения настроены в `.env`

---

**Успешной работы с API! 🚀**








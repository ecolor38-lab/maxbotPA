# 🔧 Полное руководство по решению проблемы SIGTERM

## ❓ Что означает ошибка

```
npm error signal SIGTERM
```

**SIGTERM** = SIGnal TERMinate - операционная система **убила** ваш процесс.  
Это **не ошибка в коде**, а принудительное завершение платформой.

---

## 🎯 Основные причины на сервере

### 1. 💾 **Недостаток памяти (OOM)**
   - Приложение использует > доступного RAM
   - Платформа убивает процесс
   - **Признак:** В логах может быть "out of memory"

### 2. ⏰ **Таймаут запуска (Health Check)**
   - Сервер не ответил на `/health` за 60 секунд
   - Платформа считает его мертвым
   - **Признак:** "Failed to bind to port", "Health check timeout"

### 3. 🔌 **Неправильный порт**
   - Приложение слушает не тот порт, который ожидает платформа
   - **Признак:** "Port binding failed"

### 4. 💻 **Превышение CPU лимитов**
   - Слишком много вычислений при старте
   - **Признак:** "CPU quota exceeded"

### 5. 🐛 **Краш при инициализации**
   - Ошибка в коде при запуске
   - Uncaught exception до того как сервер запустился

---

## ✅ ИСПРАВЛЕНИЯ (уже применены)

### 1. ✔️ Исправлен Procfile
**Было:**
```
web: npm run server
```

**Стало:**
```
web: node server.js
```

**Почему:** NPM не корректно передает SIGTERM в дочерний процесс Node.js.

### 2. ✔️ Graceful Shutdown в server.js
- Обработка SIGTERM и SIGINT
- Остановка планировщика перед завершением
- Timeout 30 секунд для принудительного выхода

### 3. ✔️ Защита от падений
- `uncaughtException` - не падает, логирует
- `unhandledRejection` - не падает, логирует
- Асинхронная инициализация сервисов

---

## 🔍 ДИАГНОСТИКА

### Шаг 1: Проверьте логи ПОЛНОСТЬЮ

```bash
# Render:
render logs --tail 200

# Railway:
railway logs

# Heroku:
heroku logs --tail --num 200

# VPS с PM2:
pm2 logs ai-bot --lines 200
```

**Ищите:**
- `EADDRINUSE` - порт занят
- `out of memory` - нехватка памяти
- `timeout` - таймаут запуска
- `Error:` перед SIGTERM - ошибка в коде

### Шаг 2: Проверьте переменные окружения

**Обязательные:**
```bash
PORT=3000                          # Порт (на Render/Railway часто динамический $PORT)
NODE_ENV=production
AUTO_START_SCHEDULER=true          # Опционально
```

**API ключи:**
```bash
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHANNEL_ID=@your_channel
```

### Шаг 3: Проверьте порт

На **Render/Railway/Heroku** порт динамический!

**Правильно:**
```javascript
const PORT = process.env.PORT || 3000;  // ✅ Уже есть в server.js
```

Убедитесь что переменная `PORT` **НЕ** захардкожена в `.env` на сервере!

### Шаг 4: Проверьте память

```bash
# В логах платформы ищите:
- "Memory usage: XXX MB"
- Ваш лимит (обычно 512MB на бесплатных планах)
```

---

## 🚀 РЕШЕНИЯ

### Решение 1: Деплой исправлений

```bash
# 1. Закоммитить изменения
git add Procfile
git commit -m "fix: use node directly instead of npm"
git push origin main

# 2. На сервере (если VPS):
git pull origin main
pm2 restart ai-bot
pm2 logs ai-bot --lines 50
```

### Решение 2: Увеличить память (если OOM)

**Опция A: Увеличить лимит памяти в ecosystem.config.cjs**
```javascript
max_memory_restart: '512M',  // Вместо 1G, если сервер предоставляет меньше
```

**Опция B: Оптимизировать код**
- Не храните все посты в памяти
- Очищайте старые изображения
- Используйте streaming для больших данных

### Решение 3: Отключить автозапуск планировщика

Если приложение падает при инициализации:

```bash
# .env на сервере
AUTO_START_SCHEDULER=false
```

Запустите планировщик вручную через API после старта:
```bash
curl -X POST http://your-app.com/api/scheduler/start
```

### Решение 4: Добавить задержку инициализации

Если сервис запускается слишком долго:

В `server.js` строка 344:
```javascript
await new Promise(resolve => setTimeout(resolve, 5000)); // Увеличить до 5 сек
```

### Решение 5: Проверить health check

Убедитесь что платформа проверяет правильный endpoint:

```bash
# Render/Railway settings:
Health Check Path: /health
Health Check Port: [Same as application]
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Локально:
```bash
# 1. Убедитесь что все работает
npm install
node server.js

# 2. Проверьте health check
curl http://localhost:3000/health

# 3. Отправьте SIGTERM (симуляция)
# Windows PowerShell:
Stop-Process -Name node -Force

# Linux/Mac:
kill -SIGTERM $(pgrep -f "node server.js")

# Должно быть в логах:
# "👋 Получен сигнал SIGTERM, останавливаю сервер..."
# "✅ Сервер остановлен"
```

### На сервере:
```bash
# 1. Проверьте что сервер отвечает
curl https://your-app.com/health

# 2. Проверьте логи
# Должно быть:
# "🚀 AI Business Bot Server запущен!"
# "⏰ Автозапуск планировщика..."
# "✅ Планировщик успешно запущен"

# Не должно быть:
# "npm error signal SIGTERM"
# "Error: EADDRINUSE"
# "timeout"
```

---

## 📊 ЧЕКЛИСТ

- [x] ✅ Procfile исправлен (`node server.js` вместо `npm run server`)
- [ ] 🔍 Проверены логи на наличие реальной ошибки перед SIGTERM
- [ ] 🔑 Все переменные окружения установлены
- [ ] 🔌 Порт настроен правильно (`process.env.PORT`)
- [ ] 💾 Проверено потребление памяти
- [ ] ⏰ Health check настроен на `/health`
- [ ] 🚀 Изменения задеплоены

---

## 🆘 ЧТО ДЕЛАТЬ ЕСЛИ НЕ ПОМОГЛО

### 1. Получите ПОЛНЫЕ логи
```bash
# Сохраните логи в файл
render logs > logs.txt
# ИЛИ
railway logs > logs.txt
# ИЛИ
pm2 logs --lines 500 > logs.txt
```

### 2. Ищите ключевые слова:
- Последняя строка перед `SIGTERM`
- `Error:` - ошибки в коде
- `EADDRINUSE` - порт занят
- `ENOENT` - файл не найден
- `timeout` - таймаут
- `memory` - память

### 3. Минимальный тест:
Создайте минимальный `test-server.js`:
```javascript
import express from 'express';
const app = express();
const PORT = process.env.PORT || 3000;

app.get('/health', (req, res) => {
  res.json({ status: 'ok' });
});

app.listen(PORT, '0.0.0.0', () => {
  console.log(`✅ Test server running on ${PORT}`);
});
```

Запустите его вместо основного:
```bash
# Procfile
web: node test-server.js
```

Если минимальный сервер работает - проблема в вашем коде.  
Если минимальный сервер тоже падает - проблема в конфигурации платформы.

---

## 📚 Дополнительные материалы

- [Node.js Process Signals](https://nodejs.org/api/process.html#signal-events)
- [PM2 Graceful Shutdown](https://pm2.keymetrics.io/docs/usage/signals-clean-restart/)
- [Heroku Node.js Best Practices](https://devcenter.heroku.com/articles/node-best-practices)

---

**Удачи! Ваш бот будет работать стабильно! 🚀**


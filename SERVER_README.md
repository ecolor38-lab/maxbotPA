# 🚀 Запуск Express Server

## Быстрый старт

### 1. Установка зависимостей

Express.js уже добавлен в `package.json`. Установите все зависимости:

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Основные настройки
PORT=3000
NODE_ENV=development
AUTO_START_SCHEDULER=false

# Telegram
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHANNEL_ID=@ваш_канал

# AI API ключи
OPENAI_API_KEY=ваш_ключ_openai
ANTHROPIC_API_KEY=ваш_ключ_anthropic

# Расписание (cron формат)
CRON_SCHEDULE_1=0 */3 * * *
TIMEZONE=Asia/Irkutsk
POSTS_PER_BATCH=3

# Настройки поиска
SEARCH_DAYS_BACK=7
MAX_NEWS_ITEMS=5
LANGUAGE=ru
```

### 3. Запуск сервера

```bash
npm run server
```

Сервер запустится на `http://localhost:3000`

### 4. Проверка работы

Откройте в браузере или используйте curl:

```bash
# Health check
curl http://localhost:3000/health

# Список endpoints
curl http://localhost:3000/

# Статус бота
curl http://localhost:3000/api/bot/status
```

## 📡 Доступные команды

### Локальные команды

```bash
# Запуск сервера
npm run server

# Запуск с автоперезагрузкой (для разработки)
npm run server:dev

# Запуск бота один раз (без сервера)
npm start

# Запуск планировщика (без сервера)
npm run schedule

# Сбор новостей (без сервера)
npm run collect
```

### API endpoints

После запуска сервера доступны следующие endpoints:

#### Управление ботом

```bash
# Получить статус
curl http://localhost:3000/api/bot/status

# Запустить бота (сбор новостей + публикация)
curl -X POST http://localhost:3000/api/bot/run

# Опубликовать следующий пост из очереди
curl -X POST http://localhost:3000/api/bot/publish
```

#### Контент-план

```bash
# Получить статистику
curl http://localhost:3000/api/content/stats

# Получить очередь постов
curl http://localhost:3000/api/content/queue

# Собрать новости и добавить в контент-план
curl -X POST http://localhost:3000/api/content/collect
```

#### Планировщик

```bash
# Запустить автоматический планировщик
curl -X POST http://localhost:3000/api/scheduler/start

# Получить статус планировщика
curl http://localhost:3000/api/scheduler/status
```

## 🎯 Типичные сценарии использования

### Сценарий 1: Разработка и тестирование

```bash
# 1. Запустите сервер в режиме разработки
npm run server:dev

# 2. В другом терминале соберите новости
curl -X POST http://localhost:3000/api/content/collect

# 3. Проверьте очередь
curl http://localhost:3000/api/content/queue

# 4. Опубликуйте тестовый пост
curl -X POST http://localhost:3000/api/bot/publish
```

### Сценарий 2: Автоматическая работа 24/7

```bash
# 1. Установите AUTO_START_SCHEDULER=true в .env
# 2. Запустите сервер
npm run server

# Бот автоматически:
# - Запустит планировщик при старте
# - Будет публиковать посты по расписанию
# - Будет собирать новости каждые 3 часа
```

### Сценарий 3: Ручное управление через API

```bash
# 1. Запустите сервер с AUTO_START_SCHEDULER=false
npm run server

# 2. Управляйте вручную через API
curl -X POST http://localhost:3000/api/content/collect  # Собрать новости
curl -X POST http://localhost:3000/api/bot/publish       # Опубликовать
```

## 🐛 Устранение проблем

### Ошибка: Cannot find module 'express'

```bash
# Установите зависимости
npm install
```

### Ошибка: TELEGRAM_BOT_TOKEN not found

```bash
# Создайте .env файл с токеном
echo "TELEGRAM_BOT_TOKEN=ваш_токен" > .env
```

### Проверка логов

Сервер выводит подробные логи в консоль. При ошибках смотрите:
- `❌` - критические ошибки
- `⚠️` - предупреждения
- `✅` - успешные операции

## 📚 Дополнительная документация

- [API_DOCS.md](./API_DOCS.md) - полная документация API
- [DEPLOY.md](./DEPLOY.md) - инструкции по деплою на различные платформы
- [FINAL_README.md](./FINAL_README.md) - общая документация проекта

## 🎉 Готово!

Теперь у вас есть полноценный REST API для управления ботом, который можно задеплоить на любую платформу!








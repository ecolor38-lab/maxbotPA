# 🚀 Настройка Production окружения

## 📁 Права доступа к файлам

Боту нужны права на запись в следующие папки:

```bash
/app/
├── images/       # Для сохранения изображений
├── posts/        # Для сохранения постов
└── source-stats.json  # Для статистики источников
```

### На VPS/Linux

```bash
# Перейти в папку проекта
cd /opt/maxbotPA

# Создать необходимые папки
mkdir -p images posts

# Установить права
chmod 755 images posts
chown -R $USER:$USER images posts

# Если запускаете через PM2
pm2 restart ai-bot
```

### На Render.com / Railway.app

Эти платформы используют **эфемерную файловую систему** (ephemeral FS):
- Файлы создаются при каждом деплое
- Файлы удаляются при перезапуске
- ✅ **Бот работает БЕЗ сохранения файлов**

**🆕 Умная система папок (v2.1):**
- 🔍 Автоматически пробует несколько путей:
  1. `/app/images/` (основной)
  2. `/tmp/images/` (ephemeral FS) ✅
  3. Системная временная папка (резерв)
- ✅ Находит первую доступную для записи
- 📊 Подробные логи работы

Бот автоматически:
- 🔄 Адаптируется к любой платформе
- 📤 Использует `/tmp/` на Render/Railway
- ⚠️ Работает без изображений если FS только для чтения

**См. подробности:** [IMAGE_FIX_GUIDE.md](./IMAGE_FIX_GUIDE.md)

### На Heroku

Heroku также использует ephemeral FS. Для постоянного хранения файлов:

```bash
# Вариант 1: Использовать S3 для изображений (рекомендуется)
heroku addons:create bucketeer:micro

# Вариант 2: Работать без сохранения (по умолчанию)
# Бот автоматически адаптируется
```

---

## 🔧 Переменные окружения

Убедитесь что установлены все переменные:

```bash
# Обязательные
TELEGRAM_BOT_TOKEN=your_token
TELEGRAM_CHANNEL_ID=@your_channel
ANTHROPIC_API_KEY=your_key

# Опциональные
LANGUAGE=ru
NODE_ENV=production
AUTO_START_SCHEDULER=true
```

---

## 📊 Работа без файловой системы

Бот корректно работает даже без прав на запись:

### ✅ Что работает:
- Публикация постов в Telegram
- Генерация текстов через AI
- Генерация изображений (отправляет напрямую)
- Планировщик постов
- REST API

### ⚠️ Что НЕ работает:
- Сохранение постов в файлы (не критично)
- Сохранение изображений локально (не критично)
- Статистика источников (создается в памяти)

**Это нормально для serverless/cloud платформ!**

---

## 🐳 Docker

Если используете Docker, добавьте volumes:

```yaml
version: '3.8'
services:
  ai-bot:
    build: .
    volumes:
      - ./images:/app/images
      - ./posts:/app/posts
      # Не монтируйте JSON файлы напрямую - может вызвать проблемы с правами
      # Приложение создаст их автоматически (content-plan.json, published-posts.json, source-stats.json)
    environment:
      - NODE_ENV=production
```

**⚠️ Важно:** Не монтируйте файлы конфигурации (`.json`) напрямую в Docker volumes, так как это может вызвать ошибки прав доступа. Приложение создаст эти файлы автоматически при первом запуске.

---

## 🧪 Проверка прав

Проверьте права через API:

```bash
curl http://your-server/health
```

В логах будут предупреждения если нет прав:

```
⚠️ Нет прав на сохранение файла (только чтение)
⚠️ Недостаточно прав для работы с изображениями
⚠️ Не удалось сохранить статистику (только чтение)
```

**Это НЕ ошибки - бот продолжит работу!**

---

## ✅ Быстрый чеклист

Перед деплоем на production:

- [ ] Установлены все переменные окружения
- [ ] Telegram бот добавлен в канал как админ
- [ ] API ключи валидны
- [ ] Проверен health endpoint
- [ ] Логи показывают успешные публикации

**Права на файлы НЕ обязательны для работы бота!**

---

## 🆘 Troubleshooting

### "EACCES: permission denied"

**Это нормально!** Бот работает без сохранения файлов.

Если всё же нужно сохранение на VPS:

```bash
# Проверить владельца
ls -la /opt/maxbotPA

# Исправить права
sudo chown -R $(whoami):$(whoami) /opt/maxbotPA
chmod 755 images posts
```

### Бот не публикует посты

Проблема НЕ в правах доступа. Проверьте:

```bash
# Логи
pm2 logs ai-bot

# Или
heroku logs --tail
```

Частые причины:
- ❌ Неверный TELEGRAM_BOT_TOKEN
- ❌ Бот не админ канала
- ❌ Неверный TELEGRAM_CHANNEL_ID

---

## 📝 Рекомендации

### Для VPS (есть файловая система):
✅ Создайте папки и установите права
✅ Используйте локальное сохранение

### Для Cloud (Render/Railway/Heroku):
✅ НЕ создавайте папки - они эфемерные
✅ Работайте без локального сохранения
✅ Используйте логи платформы для мониторинга

---

**Бот готов к production даже без прав на запись файлов!** 🎉


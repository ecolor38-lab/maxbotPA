# 🔧 Исправление ошибки EADDRINUSE (порт занят)

## ❌ Проблема:
```
Error: listen EADDRINUSE: address already in use :::3000
```

Порт 3000 уже занят. PM2 запускает несколько процессов одновременно.

---

## ✅ Решение (выполните на сервере):

### Шаг 1: Остановить все процессы PM2

```bash
# Показать все процессы
pm2 list

# Остановить все
pm2 delete all

# ИЛИ остановить только ai-bot
pm2 delete ai-bot
```

### Шаг 2: Проверить что порт свободен

```bash
# Найти процесс на порту 3000
lsof -i :3000

# Если есть процесс - убить его
kill -9 <PID>

# ИЛИ найти через netstat
netstat -tulpn | grep 3000
```

### Шаг 3: Обновить код

```bash
git pull origin main
```

### Шаг 4: Запустить заново

```bash
# Один раз запустить
pm2 start ecosystem.config.js

# Проверить
pm2 list
pm2 logs ai-bot
```

---

## 🚀 Полная команда (скопируйте целиком):

```bash
pm2 delete all && \
git pull origin main && \
pm2 start ecosystem.config.js && \
pm2 save && \
pm2 logs ai-bot --lines 30
```

---

## 📊 Проверка успеха:

Логи должны показать:
```
✅ Сервисы инициализированы
🚀 AI Business Bot Server запущен!
📡 Сервер слушает порт: 3000
✅ Планировщик успешно запущен
```

Без ошибок `EADDRINUSE`!

---

## 🆘 Если не помогло:

### Вариант 1: Использовать другой порт

```bash
# В .env измените:
PORT=3001
```

### Вариант 2: Убедиться что PM2 запущен правильно

```bash
# Удалить все процессы PM2
pm2 delete all
pm2 kill

# Запустить заново
pm2 start ecosystem.config.js
```

### Вариант 3: Перезагрузить сервер

```bash
sudo reboot
```

После перезагрузки:
```bash
cd /app
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```



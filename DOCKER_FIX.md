# 🐳 Исправление проблем с Docker

## ✅ Что исправлено в коде:

1. **contentPlanner.js** - теперь ищет доступную для записи директорию
2. **sourceStats.js** - то же самое
3. Автоматический fallback на `/tmp` если основная FS read-only

---

## 🔧 Решение проблемы в Docker:

### Вариант 1: Монтировать volume для данных (рекомендуется)

```bash
# Создать volume
docker volume create ai-bot-data

# Запустить контейнер с volume
docker run -d \
  --name ai-bot \
  -v ai-bot-data:/data \
  -e QWEN_API_KEY=sk-a6509b0b95d1473696c7b1548deda5fe \
  --env-file .env \
  your-image:latest
```

### Вариант 2: Монтировать локальную папку

```bash
# Создать папку для данных
mkdir -p ./data

# Дать права на запись
chmod 777 ./data

# Запустить с монтированием
docker run -d \
  --name ai-bot \
  -v $(pwd)/data:/data \
  -e QWEN_API_KEY=sk-a6509b0b95d1473696c7b1548deda5fe \
  --env-file .env \
  your-image:latest
```

### Вариант 3: Использовать /tmp (автоматически)

Код теперь автоматически использует `/tmp` если `/app` read-only.  
Просто перезапустите контейнер - должно заработать!

```bash
docker restart ai-bot
```

---

## 📝 Обновить на сервере:

```bash
# Получить обновления
git pull origin main

# Пересобрать образ (если Docker)
docker build -t ai-bot .

# Перезапустить
docker-compose down
docker-compose up -d
```

Или с PM2:
```bash
pm2 restart ai-bot
```

---

## ✅ Проверка:

Логи должны показать:
```
📂 Используется директория для данных: /tmp/ai-bot
```

Вместо ошибки доступа.



#!/bin/bash
# Скрипт для создания необходимых папок

echo "🚀 Настройка папок для AI Business Bot..."

# Определяем директорию проекта
PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$PROJECT_DIR" || exit 1

echo "📁 Создаю папки..."

# Создаем папки
mkdir -p images
mkdir -p posts

# Устанавливаем права
chmod 755 images posts

echo "✅ Папки созданы:"
ls -la | grep -E "(images|posts)"

echo ""
echo "🔍 Проверка прав:"
test -w images && echo "✅ images - запись разрешена" || echo "❌ images - нет прав на запись"
test -w posts && echo "✅ posts - запись разрешена" || echo "❌ posts - нет прав на запись"

echo ""
echo "✨ Готово! Теперь можно запустить бота:"
echo "   pm2 restart ai-bot"
echo "   или"
echo "   npm start"





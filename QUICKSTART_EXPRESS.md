# 🚀 Быстрый старт с Express Server

## Что было добавлено

✅ Express.js сервер с REST API  
✅ Полная документация  
✅ Примеры использования на JS, Python, Bash  
✅ Готовность к деплою на любую платформу  

## 3 простых шага для запуска

### Шаг 1: Установите зависимости

```bash
npm install
```

> **Проблемы с установкой?** См. [INSTALL_EXPRESS.md](./INSTALL_EXPRESS.md)

### Шаг 2: Настройте .env

Создайте файл `.env` в корне проекта:

```env
# Минимальная конфигурация
PORT=3000
NODE_ENV=development
AUTO_START_SCHEDULER=false

# Ваши API ключи
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHANNEL_ID=@ваш_канал
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-api03-...

# Расписание
CRON_SCHEDULE_1=0 */3 * * *
TIMEZONE=Asia/Irkutsk
POSTS_PER_BATCH=3
```

### Шаг 3: Запустите сервер

```bash
npm run server
```

Вы должны увидеть:

```
============================================================
🚀 AI Business Bot Server запущен!
============================================================
📡 Сервер слушает порт: 3000
🌐 URL: http://localhost:3000
💚 Health Check: http://localhost:3000/health
📖 API Docs: http://localhost:3000/
============================================================
```

## Проверка работы

Откройте в браузере: http://localhost:3000

Или используйте curl:

```bash
curl http://localhost:3000/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": "2025-12-25T...",
  "uptime": 12.34
}
```

## Первая публикация

### Вариант 1: Через браузер

Откройте http://localhost:3000 и используйте curl команды из документации

### Вариант 2: Через примеры

```bash
# Полный рабочий процесс (рекомендуется для первого раза)
node examples/api-usage.js workflow
```

Это автоматически:
1. Проверит подключение
2. Соберет новости
3. Опубликует тестовый пост
4. Запустит планировщик

### Вариант 3: Вручную через API

```bash
# 1. Соберите новости
curl -X POST http://localhost:3000/api/content/collect

# 2. Подождите 30 секунд

# 3. Проверьте очередь
curl http://localhost:3000/api/content/queue

# 4. Опубликуйте пост
curl -X POST http://localhost:3000/api/bot/publish
```

## Автоматическая работа 24/7

### Локально

```bash
# Установите в .env
AUTO_START_SCHEDULER=true

# Перезапустите сервер
npm run server

# Теперь бот автоматически публикует посты по расписанию!
```

### На сервере (Render/Railway/Heroku)

Следуйте инструкциям в [DEPLOY.md](./DEPLOY.md)

**Краткая версия для Render.com:**

1. Создайте аккаунт на [render.com](https://render.com)
2. Создайте "Web Service" и подключите GitHub репозиторий
3. Настройте:
   - Build Command: `npm install`
   - Start Command: `npm run server`
4. Добавьте переменные окружения из `.env`
5. Deploy!

## Полезные команды

```bash
# Локальная разработка
npm run server:dev      # С автоперезагрузкой

# Старые команды (все еще работают)
npm start              # Один пост
npm run schedule       # Планировщик без API

# Примеры
node examples/api-usage.js health     # Health check
node examples/api-usage.js stats      # Статистика
node examples/api-usage.js publish    # Опубликовать
node examples/api-usage.js workflow   # Полный цикл
```

## Документация

Для более подробной информации:

| Документ | Описание |
|----------|----------|
| [SERVER_README.md](./SERVER_README.md) | Подробное руководство по серверу |
| [API_DOCS.md](./API_DOCS.md) | Полная документация REST API |
| [DEPLOY.md](./DEPLOY.md) | Деплой на Render, Railway, Heroku, VPS |
| [examples/README.md](./examples/README.md) | Примеры использования |
| [EXPRESS_INTEGRATION_SUMMARY.md](./EXPRESS_INTEGRATION_SUMMARY.md) | Полный обзор изменений |

## Основные API endpoints

После запуска сервера доступны:

```bash
# Информация
GET  /                          # Список endpoints
GET  /health                    # Health check

# Управление ботом
GET  /api/bot/status            # Статус
POST /api/bot/run               # Запустить бота
POST /api/bot/publish           # Опубликовать пост

# Контент-план
GET  /api/content/stats         # Статистика
GET  /api/content/queue         # Очередь постов
POST /api/content/collect       # Собрать новости

# Планировщик
GET  /api/scheduler/status      # Статус
POST /api/scheduler/start       # Запустить
```

## Что дальше?

### Для разработки:
1. ✅ Сервер запущен локально
2. ✅ Тестовый пост опубликован
3. → Настройте расписание в `.env`
4. → Используйте API для управления

### Для продакшена:
1. ✅ Все работает локально
2. → Выберите платформу (Render/Railway/Heroku)
3. → Следуйте [DEPLOY.md](./DEPLOY.md)
4. → Настройте `AUTO_START_SCHEDULER=true`
5. → Бот работает 24/7! 🎉

## Проблемы?

**npm install не работает**  
→ См. [INSTALL_EXPRESS.md](./INSTALL_EXPRESS.md)

**Сервер не запускается**  
→ Проверьте `.env` файл и порт 3000

**Бот не публикует в Telegram**  
→ Проверьте `TELEGRAM_BOT_TOKEN` и права бота в канале

**API возвращает ошибки**  
→ Проверьте логи сервера в консоли

**Другие вопросы**  
→ Создайте Issue в GitHub

---

**🎉 Готово! Теперь у вас есть полнофункциональный API для управления ботом!**

**Следующий шаг:** Запустите `node examples/api-usage.js workflow` 🚀








import Anthropic from '@anthropic-ai/sdk';
import OpenAI from 'openai';
import { getOptimizedWeights } from './contentOptimizer.js';

// Типы постов с разными промптами и стилями
const POST_TYPES = {
  // Короткая новость — как у Technosplit: эмодзи + заголовок + 2-3 предложения + вопрос
  news_flash: {
    weight: 40,
    label: 'новость',
    system: `Ты — редактор популярного русскоязычного канала "Нейро.Новости" в мессенджере MAX.
Твой стиль как у лучших ТГ-каналов: коротко, ярко, по делу.
Ты ВСЕГДА пишешь на русском языке. Английские термины переводишь.
Тон: уверенный, немного дерзкий, с собственным мнением. Не сухая новость, а новость с характером.`,

    prompt: (articles) => `Напиши КОРОТКИЙ пост-новость (250-400 символов).

Новости:
${articles}

ФОРМАТ (соблюдай точно):
1. Одно эмодзи + **жирный заголовок** (до 8 слов)
2. Пустая строка
3. 2-3 предложения с фактами. Конкретные цифры, имена, даты.
4. Пустая строка
5. Короткая авторская реплика ИЛИ вопрос читателям (1 предложение)

ПРАВИЛА:
- ТОЛЬКО русский язык
- Заголовок обязательно **жирный** (в двойных звёздочках)
- Один абзац = одна мысль
- Без хештегов, без ссылок
- Без "Подписывайтесь" и подобного`
  },

  // Аналитика — глубокий разбор одной темы с мнением автора
  analysis: {
    weight: 30,
    label: 'аналитика',
    system: `Ты — AI-аналитик и редактор канала "Нейро.Новости" в MAX мессенджере.
Твоя суперсила — находить неочевидные связи и объяснять сложное просто.
Пишешь на русском. Тон: умный, но не занудный. Как будто объясняешь другу за кофе.
Ты не боишься высказывать мнение и делать прогнозы.`,

    prompt: (articles) => `Напиши АНАЛИТИЧЕСКИЙ пост (600-1000 символов).

Новости для анализа:
${articles}

ФОРМАТ:
1. Эмодзи + **жирный заголовок-утверждение** (провокационный или неочевидный)
2. Пустая строка
3. Контекст: что произошло (2-3 предложения с фактами и цифрами)
4. Пустая строка
5. Анализ: почему это важно, что за этим стоит (2-3 предложения)
6. Пустая строка
7. Твой вывод или прогноз: что будет дальше (1-2 предложения, смелое мнение)

ПРАВИЛА:
- ТОЛЬКО русский язык
- Заголовок **жирный**
- Выбери ОДНУ самую интересную тему из списка и раскрой её глубоко
- Не пересказывай — анализируй
- Без хештегов, без ссылок
- Заверши вопросом или провокационным утверждением`
  },

  // Подборка/дайджест — несколько новостей списком
  digest: {
    weight: 15,
    label: 'дайджест',
    system: `Ты — редактор канала "Нейро.Новости" в MAX.
Пишешь дайджесты — краткие обзоры нескольких новостей с остроумными комментариями.
Стиль: энергичный, информативный. Каждый пункт — самостоятельная микро-новость.
Всё на русском языке.`,

    prompt: (articles) => `Напиши ДАЙДЖЕСТ из 3-5 новостей (500-800 символов).

Новости:
${articles}

ФОРМАТ:
1. 📰 **Дайджест: что произошло в мире ИИ** (или похожий заголовок)
2. Пустая строка
3. Нумерованный список (3-5 пунктов):
   Каждый пункт: эмодзи + **жирный мини-заголовок** — одно предложение суть
4. Пустая строка
5. Вопрос: "Какая новость удивила больше всего?" или похожий

ПРАВИЛА:
- ТОЛЬКО русский язык
- Каждый пункт — отдельная новость из списка
- Максимум 1-2 строки на пункт
- Без хештегов, без ссылок`
  },

  // Совет / инсайт — практическая польза для читателя
  tip: {
    weight: 15,
    label: 'совет',
    system: `Ты — AI-эксперт и редактор канала "Нейро.Новости" в MAX.
Ты помогаешь обычным людям использовать ИИ в жизни и работе.
Пишешь практичные, полезные посты, которые хочется сохранить.
Всё на русском, простым языком, без жаргона.`,

    prompt: (articles) => `На основе этих новостей напиши ПРАКТИЧЕСКИЙ пост-совет (400-700 символов).

Новости (используй как контекст):
${articles}

ФОРМАТ:
1. 💡 **Жирный заголовок** с пользой для читателя (типа "Как ... за 5 минут")
2. Пустая строка
3. Коротко: что нового появилось / изменилось (1-2 предложения)
4. Пустая строка
5. Практический совет: что конкретно может сделать читатель (2-3 предложения)
6. Пустая строка
7. Мотивирующая фраза или вопрос "А вы уже пробовали?"

ПРАВИЛА:
- ТОЛЬКО русский язык
- Заголовок **жирный**
- Фокус на ПОЛЬЗЕ для обычного человека
- Без хештегов, без ссылок
- Пост должен быть полезным, а не просто новостным`
  },

  // Прогноз — провокационный формат
  prediction: {
    weight: 10,
    label: 'прогноз',
    system: `Ты — AI-футуролог и редактор канала "Нейро.Новости" в MAX.
Ты делаешь смелые прогнозы о будущем технологий на основе текущих трендов.
Твой стиль: уверенный, провокационный, но обоснованный фактами.
Всё на русском языке.`,

    prompt: (articles) => `На основе этих новостей напиши ПРОГНОЗ (400-600 символов).

Новости:
${articles}

ФОРМАТ:
1. 🔮 **Прогноз: [смелое утверждение о будущем]**
2. Пустая строка
3. Почему я так считаю (2-3 предложения с фактами из новостей)
4. Пустая строка
5. Что это значит для обычного человека (1-2 предложения)
6. Пустая строка
7. Провокационный вопрос: "Согласны? Или я ошибаюсь?"

ПРАВИЛА:
- ТОЛЬКО русский язык
- Заголовок **жирный**
- Прогноз должен быть КОНКРЕТНЫМ (срок, что именно произойдёт)
- Без хештегов, без ссылок`
  },

  // Hot take — непопулярное мнение
  hot_take: {
    weight: 10,
    label: 'hot take',
    system: `Ты — дерзкий AI-обозреватель канала "Нейро.Новости" в MAX.
Ты не боишься высказывать непопулярные мнения и идти против течения.
Твой стиль: провокационный, но аргументированный. Ты вызываешь дискуссию.
Всё на русском языке.`,

    prompt: (articles) => `На основе этих новостей напиши HOT TAKE — непопулярное мнение (400-700 символов).

Новости:
${articles}

ФОРМАТ:
1. 🔥 **Непопулярное мнение: [контринтуитивное утверждение]**
2. Пустая строка
3. Контекст: что все думают (1-2 предложения)
4. Пустая строка
5. Почему я не согласен (2-3 предложения с аргументами)
6. Пустая строка
7. Вызов: "Попробуйте меня переубедить" или похожий

ПРАВИЛА:
- ТОЛЬКО русский язык
- Заголовок **жирный**
- Мнение должно быть действительно НЕПОПУЛЯРНЫМ, но обоснованным
- Без хештегов, без ссылок`
  },

  // Серийный контент — часть цикла
  series: {
    weight: 5,
    label: 'серия',
    system: `Ты — редактор канала "Нейро.Новости" в MAX.
Ты пишешь серийный контент — посты, которые являются частью цикла.
Каждый пост самодостаточен, но вызывает желание прочитать продолжение.
Всё на русском языке.`,

    prompt: (articles) => `На основе этих новостей напиши пост как часть серии (400-700 символов).

Новости:
${articles}

ФОРМАТ:
1. 📌 **[Название серии] | Часть N** (придумай интересную серию)
2. Пустая строка
3. Основной контент: факты, анализ, советы (3-4 предложения)
4. Пустая строка
5. Тизер: "В следующей части: ..." (интрига)

ПРАВИЛА:
- ТОЛЬКО русский язык
- Заголовок **жирный**
- Пост должен быть самодостаточным, но с крючком на продолжение
- Без хештегов, без ссылок`
  },

  // Рекламный/спонсорский пост
  sponsored: {
    weight: 0,
    label: 'реклама',
    system: `Ты — редактор канала "Нейро.Новости" в MAX.
Ты пишешь нативные рекламные интеграции, которые выглядят как обычный полезный контент.
Стиль: информативный, полезный, без навязчивой рекламы.
Всё на русском языке.`,

    prompt: (articles) => `Напиши НАТИВНЫЙ рекламный пост (400-700 символов).

Бриф:
${articles}

ФОРМАТ:
1. 📌 **Партнёрский материал**
2. Пустая строка
3. Полезный контент с естественным упоминанием продукта (3-5 предложений)
4. Пустая строка
5. Мягкий CTA (без агрессивных призывов)

ПРАВИЛА:
- ТОЛЬКО русский язык
- Обязательна пометка "Партнёрский материал"
- Реклама должна быть ПОЛЕЗНОЙ для читателя
- Без хештегов`
  }
};

export class AISummarizer {
  constructor(config) {
    this.config = config;
    this.anthropic = config.anthropic?.apiKey
      ? new Anthropic({ apiKey: config.anthropic.apiKey })
      : null;
    this.openai = config.openai?.apiKey ? new OpenAI({ apiKey: config.openai.apiKey }) : null;
    this.lastPostType = null;
  }

  pickPostType() {
    const optimizedWeights = getOptimizedWeights();
    const types = Object.entries(POST_TYPES).map(([key, type]) => ({
      key,
      ...type,
      weight: optimizedWeights[key] ?? type.weight
    }));

    const available = types.filter((t) => t.key !== this.lastPostType && t.key !== 'sponsored' && t.weight > 0);
    const availableWeight = available.reduce((sum, t) => sum + t.weight, 0);

    let rand = Math.random() * availableWeight;
    for (const type of available) {
      rand -= type.weight;
      if (rand <= 0) {
        this.lastPostType = type.key;
        return type;
      }
    }

    const fallback = available[0];
    this.lastPostType = fallback.key;
    return fallback;
  }

  async generateSummary(articles, forceType = null) {
    const postType = forceType
      ? { key: forceType, ...POST_TYPES[forceType] }
      : this.pickPostType();

    this.lastPostType = postType.key;

    console.log(`🤖 Генерирую пост [${postType.label}] из ${articles.length} статей...`);

    const articlesText = articles
      .map((a, i) => `${i + 1}. ${a.title}\n   ${a.snippet || ''}`)
      .join('\n\n');

    const systemPrompt = postType.system;
    const userPrompt = postType.prompt(articlesText);

    try {
      if (this.anthropic) {
        return await this.callClaude(systemPrompt, userPrompt);
      } else if (this.openai) {
        return await this.callOpenAI(systemPrompt, userPrompt);
      }
    } catch (error) {
      console.log('⚠️ Ошибка API:', error.message);
    }

    return this.getDemoSummary();
  }

  async callClaude(system, prompt) {
    const response = await this.anthropic.messages.create({
      model: this.config.anthropic.model || 'claude-sonnet-4-20250514',
      max_tokens: 1500,
      system: system,
      messages: [{ role: 'user', content: prompt }],
      temperature: 0.8
    });
    return response.content[0].text;
  }

  async callOpenAI(system, prompt) {
    const response = await this.openai.chat.completions.create({
      model: this.config.openai.model || 'gpt-4-turbo-preview',
      messages: [
        { role: 'system', content: system },
        { role: 'user', content: prompt }
      ],
      max_tokens: 1500,
      temperature: 0.8
    });
    return response.choices[0].message.content;
  }

  getDemoSummary() {
    return `🚀 **ИИ-агенты захватывают рабочие процессы**

Notion, Microsoft и Google одновременно запустили автономных ИИ-ассистентов. Они работают 24/7 без участия человека: планируют встречи, пишут отчёты, анализируют данные.

Это больше не «попроси ChatGPT» — это полноценные цифровые сотрудники. Рынок ИИ-агентов вырастет до $65 млрд к 2028 году.

А вы бы доверили ИИ-агенту свою рутину?`;
  }
}
